/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DepartementFilterPipe } from './departement-filter.pipe';

describe('Pipe: DepartementFiltere', () => {
  it('create an instance', () => {
    let pipe = new DepartementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
