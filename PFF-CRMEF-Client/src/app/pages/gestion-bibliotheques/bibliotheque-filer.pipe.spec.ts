/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BibliothequeFilerPipe } from './bibliotheque-filer.pipe';

describe('Pipe: BibliothequeFilere', () => {
  it('create an instance', () => {
    let pipe = new BibliothequeFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
