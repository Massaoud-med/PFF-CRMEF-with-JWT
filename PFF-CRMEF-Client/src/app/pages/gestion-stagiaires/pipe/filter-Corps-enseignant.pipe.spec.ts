/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterCorpsEnseignantPipe } from './filter-Corps-enseignant.pipe';

describe('Pipe: FilterCorpsEnseignante', () => {
  it('create an instance', () => {
    let pipe = new FilterCorpsEnseignantPipe();
    expect(pipe).toBeTruthy();
  });
});
