/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StagiaireFilterFilierPipe } from './stagiaire-filter-filier.pipe';

describe('Pipe: StagiaireFilterFiliere', () => {
  it('create an instance', () => {
    let pipe = new StagiaireFilterFilierPipe();
    expect(pipe).toBeTruthy();
  });
});
