/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DragDropDirectiveDirective } from './DragDropDirective.directive';

describe('Directive: DragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
